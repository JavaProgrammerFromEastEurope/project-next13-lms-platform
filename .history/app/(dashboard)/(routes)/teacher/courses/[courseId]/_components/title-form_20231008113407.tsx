'use client'

interface TitleFormProps {
	initialData: {
		title: string;
	};
	courseId: string;
};

export const TitleForm = (
	initialData,
	courseId
) => {
	return (
		<div>
			Title Form
		</div>
	)
}