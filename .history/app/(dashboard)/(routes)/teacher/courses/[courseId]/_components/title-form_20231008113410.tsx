'use client'

interface TitleFormProps {
	initialData: {
		title: string;
	};
	courseId: string;
};

export const TitleForm = (
	initialData,
	courseId
)L TitleF => {
	return (
		<div>
			Title Form
		</div>
	)
}